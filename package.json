{
    "name": "invaritech-contact-api",
    "version": "2.0.0",
    "description": "Contact form endpoint for invaritech.ai, running on Cloudflare Workers",
    "private": true,
    "type": "module",
    "scripts": {
        "start": "wrangler dev",
        "deploy": "wrangler deploy",
        "typecheck": "tsc --noEmit",
        "test": "node --experimental-strip-types --test tests/*.test.mjs"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^5.20260815.1",
        "typescript": "^5",
        "wrangler": "^4"
    },
    "engines": {
        "node": ">=22"
    }
}
